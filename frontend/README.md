# Zanlink React Frontend

React frontend for the Zanlink Document Flow System.

## Run

```bash
npm install
npm run dev
```

The app expects the Flask API at `http://localhost:5000`. To change it, create `.env`:

```bash
VITE_API_URL=http://localhost:5000
```
